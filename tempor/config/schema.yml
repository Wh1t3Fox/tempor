type: object
properties:
  providers:
    type: array
    items:
      $ref: '#/definitions/provider'
  default:
    type: string
    enum:
      - digitalocean
      - linode
      - vultr
      - aws
definitions:
  provider:
    type: object
    required:
      - name
      - region
      - image
      - api_token
    properties:
      name: 
        type: string
      region:
        type: string
