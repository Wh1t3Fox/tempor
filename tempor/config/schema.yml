type: object
properties:
  providers:
    type: array
    items:
      $ref: '#/definitions/provider'
  config:
    type: object
definitions:
  provider:
    type: object
    required:
      - name
      - region
      - image
      - resources
      - api_token
    properties:
      name: 
        type: string
      region:
        type: string
      resources:
        type: string
